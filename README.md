# isomorphic-path
A wrapper module that exposes node's path API in the browser and node contexts.

Supported Methods:
 * normalize
 * isAbsolute
 * join
 * dirname
 * basename
 * extname
 * format
 * parse
